/**
 * Standard suffix appended to user-facing error messages.
 * Provides a consistent recovery prompt across all error states in the application.
 */
export const ERROR_SUFFIX =
  'Please try again. If the problem persists, contact support.';
