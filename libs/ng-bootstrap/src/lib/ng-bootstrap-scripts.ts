// Reserved for library-level script initialization. This module is loaded
// dynamically at app startup by provideRnForgeBootstrapConfig, after Bootstrap
// and bootstrap-table are registered — add future side-effect code here without
// touching the config provider.
export {};
